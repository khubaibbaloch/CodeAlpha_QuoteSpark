<?xml version="1.0" encoding="utf-8"?>
<resources>

    <style name="Theme.QuoteSpark" parent="android:Theme.Material.Light.NoActionBar">
        <!-- Customize your main app theme here -->
        <item name="android:windowSplashScreenBackground">@color/white</item>
        <item name="android:windowSplashScreenAnimatedIcon">@drawable/quote_spark</item>
        <item name="android:windowSplashScreenIconBackgroundColor">@color/white</item>

        <!-- Keep status bar transparent during splash -->
        <item name="android:statusBarColor">@android:color/transparent</item>
    </style>
</resources>